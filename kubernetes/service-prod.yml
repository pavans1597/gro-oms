apiVersion: v1
kind: Service
metadata:
  name: oauth-service-prod
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 9090
      name: http
  selector:
    app: oauth-service-prod
  type: LoadBalancer
  #type: ClusterIP
#---
#apiVersion: networking.k8s.io/v1
#kind: Ingress
#metadata:
#  name: oauth-service-prod
#  annotations:
#    cert-manager.io/cluster-issuer: letsencrypt-console
#spec:
#  ingressClassName: console-nginx
#  tls:
#    - hosts:
#        - console.groyyo.com
#      secretName: tls-secret-console
#  rules:
#    - host: console.groyyo.com
#      http:
#        paths:
#          - path: /oauth-service-prod
#            pathType: Prefix
#            backend:
#              service:
#                name: oauth-service-prod
#                port:
#                  number: 80

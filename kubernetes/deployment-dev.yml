apiVersion: apps/v1
kind: Deployment
metadata:
  name: oms-service
spec:
  selector:
    matchLabels:
      app: oms-service
  replicas: 1
  template:
    metadata:
      labels:
        app: oms-service
        group: service-dev
    spec:
      containers:
        - image: groyyoOrgServiceRegistry.azurecr.io/oms-service-dev:latest
          name: oms-service
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "500Mi"
              cpu: "500m"
          ports:
            - containerPort: 80
